# Mockups

> 返回 [总设计纪要](../overview.md)

## interface-spec-v1.html

2026-08-11 界面规格定稿时的一次性快照，**已冻结，不再同步维护**。直接用浏览器打开即可，无依赖、无构建。

它画了六屏：工作区（三区 + 待办三级）、报告页（单列长页 + 时间轴）、Markdown 导出样例、长图分页示意、新建调查面板、调查切换栏。

> ⚠️ 快照里的文案还是改名前那套（案件 / 立案 / 结案 / 调查台），shell-v2 / home-v3 里的「排查」也已改叫「调查」。它是冻结件，不跟着改；当前词汇以 [overview.md](../overview.md) §0 与 [ui.md](../ui.md) 为准。

**规格正文在 [ui.md](../ui.md) 与 [overview.md](../overview.md)，那两份才是维护对象。** 进入开发后一律以代码为准——这个文件只回答"当时长什么样"，不回答"应该怎么实现"。两者出现分歧时，快照是错的那个。

不要为了让它跟上代码而去改它。真要重画就新建 `interface-spec-v2.html`，把旧的留着。

## shell-v2.html

2026-08-14 外壳导航的方案件，**已按它实现**（rail + 首页 / 工作区 / 历史调查 / 设置）。直接用浏览器打开，无依赖、无构建；顶部那条控制栏是 mockup 自己的，不属于方案。

它只回答"当时长什么样"。**规格已经落进 [ui.md](../ui.md) §0 与 §8.5，实现以代码为准**；三者有分歧时这份是错的那个。同 v1，不要为了让它跟上代码而去改它——真要重画就新建 v3。

定下来的三条与旧稿不同、已经改进 ui.md 的：

- rail 不写字、不带计数，参照 `~/Projects/duetlens` 的 `AppRail`
- 切换调查并到历史调查页，工作区里那排 chip 撤掉（推翻旧 §8.3「不另开面板」那半句）
- 跨案待办退化成 rail 上一个暖色圆点，不是数字

## home-v3.html

2026-08-14 首页视觉打磨的方案件，**已按它实现**。直接用浏览器打开，无依赖、无构建；底部那段「动了什么」是给当时决策用的说明，不属于方案本身。

当时摆了三个方向——A 提问台（窄单栏、问题输入是唯一主角）、B 双栏工作台、C 续接优先（顶部续接条 + 新建收起）——选了 B。另外两份没有留：真实 UI 一出来它们就归零，留着只会有人拿去当依据。

它只回答"当时长什么样"。**规格已经落进 [ui.md](../ui.md) §8.1 与 §8.5，实现以代码为准**；三者有分歧时这份是错的那个。同 v1 与 shell-v2，不要为了让它跟上代码而去改它。

定下来的三条与旧稿不同、已经改进 ui.md 的：

- 项目起点从新建面板里的第一个字段提到页头，做成常驻的一条——它是一种模式（真项目 / 演示）不是一个表单字段
- 首页是左右两栏：左边写新的，右边接上次。窄于 1080px 退回单栏
- 四个状态词不再是同一个灰；运行中带一处呼吸点，「当前那一次」在右栏顶上单独占一张卡

色板与 §5 的硬线一个都没动。新增的 CSS 变量只有同色更淡的一档（`--line-soft` / `--dim-2` / `--tint`），不是新颜色。

⚠️ **这份稿画的外壳已经过时**：它还是「rail 通到底、标记在 rail 上」。落地时发现 rail 让不开 macOS 交通灯，外壳改成了「整幅顶栏 + 它下面的 rail」，标记也搬进了顶栏（[ui.md](../ui.md) §0）。左右两栏那一半仍然作数。
