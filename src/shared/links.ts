/**
 * 项目在哪。**唯一出处**：设置屏的外链与官网（`site/`）都读这一份。
 *
 * 只收口"项目在哪"，不收口仓库里的路径——releases / CHANGELOG / LICENSE 由用它的那侧
 * 自己往后拼。那几条会随仓库结构变，而这三条不会。
 */
export const PROJECT_LINKS = {
  repo: 'https://github.com/xieziyu/inquestry',
  author: 'https://github.com/xieziyu',
} as const;
