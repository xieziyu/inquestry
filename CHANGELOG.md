# 更新日志

本文件只记**对使用者可见**的变化。内部重构、文档与 CI 调整不单列，查 `git log`。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)；`0.x` 阶段 minor 位即破坏性变更位。

## [0.2.0] - 2026-08-20

新建的调查会自己跑起来；排查过程中看得见此刻在做什么。

- **新建调查后自动跑第一轮。** 之前建完停在起点卡上不动——唯一的启动入口是输入框的发送按钮，而它在输入为空时是禁用的，界面上也没有任何东西说明在等什么。现在建完即跑；第一轮起不来会明说（横幅 + 会话标记为崩溃），草稿留在输入框里，不再悄悄排进没人看的队列。(#1)
- **描述里带链接不再把标题打回兜底。** 取标题那一趟现在一个工具都不给：它只读你写下的那段文字，链接背后是什么、文件里写了什么都不去够——那是调查本身的事。之前留着一个工具入口，模型见到链接就想去抓，把这一趟的轮次用光，标题静默停在兜底那句，界面上没有任何东西说这一趟挂过。描述短到只剩一个链接时，标题会取里面认得出的标识（issue 编号、traceId、服务名、用户 ID）。(#9)
- **看得见此刻在做什么。** 运行中的步骤卡上有一条心跳（工具名、已耗时、调用与证据计数），运行中的子 agent 在支线头上挂一个提示，HUD 上有最后更新时间。秒数由界面自己走，不等快照——一次 90 秒的工具调用本来什么都不推。(#2)
- **人工回填可以拒绝。** 回填卡上多一个「不执行」：自己也没这个权限、或这条不该在生产上跑，都可以退回去，理由选填。agent 收到的是一次明确的拒绝，不会当成「查了但没数据」接着往下推，也不会把同一条原样再问一遍；轨道上那次调用记为被拒，不是跑完。(#4)
- **会话断在半路时，未决的东西都会收口。** 之前流断开或崩溃只收支线，待回填、待放行与在途调用都留在原地：一次调用会一直显示运行中直到下次启动，报告里的调用数也跟着偏高。(#3)
- **支线旁白只说轨道上看不见的事。** 跑完并留下结论的支线不再在对话带里被重复念一遍——那句话已经写在卡面上；一次调用都没打、轨道上根本看不到的那条才需要旁白托着。(#5)
- **报告排版收敛。** 根因按正文排版，不再被放大加粗成一堵字；纸面上的印记缩成一个小标签。版式不替你划重点。(#7, #8)
- **修复：舞台上的抓手光标盖住了可选中的文字。**(#6)

**升级须知**：本轮没有数据库迁移，schema 仍是 v7，与 0.1.1 是同一份库。降级回 0.1.1 也照常打开：被拒绝的回填在库里就是一次 `denied` 调用，旧版本认得它，只是界面上没有「不执行」那个手势。

## [0.1.1] - 2026-08-19

修 0.1.0 一打开就连不上 Claude 的问题。

- **修复：正式包里调查起不来。** agent 用的 CLI 被封在 asar 归档里，spawn 不出来（`spawn ENOTDIR`）。表现是新建调查面板的 Backend 与模型列表空着、点「开始排查」没有反应。只影响正式包，从源码跑的不受影响。
- **不再要求本机装 Claude Code。** 应用自带 CLI，需要的只是一个登录过的 Claude 账号（首次登录仍在终端里做一次 `claude auth login`）。设置页「关于」直接显示登录状态与账号；环境提示也换成了这件事本身，不再报「未找到 claude 可执行文件」。

## [0.1.0] - 2026-08-16

首个公开版本。

- **完整证据留存**：agent 排查过程中每一次工具调用的原始输入/输出由 harness 完整落库，agent 的文字只做索引和判断；复核一项调查靠证据本身，不靠重跑。
- **时间线即控制面**：排查实时可见，节点上直接放行 / 改写 / 拒绝 / 接管 / 停止；`ask_operator` 把要凭据或后果须由人担的操作交回给你执行，并要求 agent 先写预期再看结果。
- **两条时间线**：排查时间线按 agent 行动顺序，系统时间线按事件真实发生时间重排，同一批证据的两次投影。
- **结论可被推翻**：被推翻的结论连同证据强制留在报告里，不造一路顺利的假历史。
- **报告是投影**：章节从库里投影而来，与过程天然一致；Markdown 与长图两种导出。
- **按调查检索**：一次调查跨多个会话；数据全部落本机 SQLite，大 payload 内容寻址落盘。
- **走 Claude 订阅**：spawn 本机 `claude` CLI 继承登录凭据，已有 skill 与 MCP 配置照常加载。
- **应用内自动更新**：后台下载、退出时安装；设置页可看状态、可手动重启生效。
