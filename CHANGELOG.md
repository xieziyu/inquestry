# 更新日志

本文件只记**对使用者可见**的变化。内部重构、文档与 CI 调整不单列，查 `git log`。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)；`0.x` 阶段 minor 位即破坏性变更位。

## [0.4.0] - 2026-08-25

老版本不再把新版本写过的库当成降级挪走；顺手修掉真实排查里撞见的几处。

- **老版本打得开新版本写过的库。** 升级到 0.3.0 之后曾经看着像所有调查都没了：`/Applications` 里那份 0.2.0 被启动过一次，它认定这份已经迁到 v9 的库「比自己新」，于是改名成 `inquestry.db.v9.<时间戳>.bak` 另建了一个空库，一分钟后装上的 0.3.0 就接着那个空库跑，再没有任何东西指回那份归档。**挪库对「只是读者旧了」这种情况是错的答案**——库是好的。现在 `user_version` 记的不再是版本号，而是**这份库最低要第几版才读得动**，真实版本移到 `meta` 表；每一级迁移必须声明自己是 additive（新表、可空列、新索引、新事件类型——不抬最低读者，老版本照旧打得开，新列它留空）还是 breaking（改 CHECK、改字段语义，或给已有列加一档新枚举值——抬）。最后那种最容易漏：老版本没有那一档的分支，它不会报错，只会把那些行画错。真的太新的库现在**一个字节都不动**：应用停在启动失败屏上说明原因，而那块屏自己会检查更新并装上（之前 updater 建在数据库打开之后，启动失败就等于它压根没被建出来）。另外开发与本地 `npm run package` 的包从此各用自己的 userData 目录，不再和已安装的正式版共用一份库——开发中的 schema 当初就是这么写进正式库的。(#25)
- **修复：案件卡的标题停在截断的兜底句。** 取标题那一趟不是答不上来，是**卡在扩展思考里超时**：同一段问题描述，默认自适应思考端到端 41 秒（其中 39 秒花在思考上），关掉思考只要 4.8 秒，而这一趟的超时是 30 秒——于是它每次都被打断、收敛成「问不到」，卡面上留下的就是描述第一行的 40 字截断，界面上没有任何东西说这一趟跑过。这趟只是把已经写好的文字压缩一遍，没有什么可想的，现在明确关掉思考。(#27)
- **修复：详情抽屉滚不动。** 舞台的滚轮监听挂在整块画布上并且无条件吃掉默认行为，而抽屉与待办层虽然钉在视口上、DOM 上仍是画布的后代，于是它们的原生滚动被吃掉，滚轮改去平移画布。现在滚轮落在这两层里就放行；落在卡片上仍然平移——卡片本来就长在画布上。(#27)
- **尾卡不再看着像已经到站。** 未定稿与可定稿的尾卡改成暂定形态：虚线边、填充大部分退掉，透出底下的点阵，沿用「这张卡还没落定」已有的写法，而不是给主干连线再添一种含义。远景（约 30% 以下缩放）仍恢复成实线实底——那个尺度上 1px 的缝隙看不见了，再空着就成了一个洞。(#27)
- **修复：案件列表把刚结案的那件排到了后面。** 排序原本是「未结案的优先，再按最近活动」，于是刚封存的案件排在几天没动静的进行中案件下面；首页那条轨道读起来是时间线，这样等于让时间轴说了假话。现在只按最近活动排，三处列表（最近、历史、搜索结果）共用同一条规则。进行中的案件靠置顶、「等你」徽标和轨道圆点保持可见，不依赖排序。(#26)

**升级须知**：schema 仍是 v9，没有迁移，与 0.3.0 是同一份库。本版起 `user_version` 里写的是最低读者（9），真实版本记在 `meta` 表里。**降级回 0.3.0 照常打开，调查一件不少**：它看的 `user_version` 正好是 9，与它自己的版本号对得上——这正是这一版要保证的事。

## [0.3.0] - 2026-08-21

报告能交出一份名单或一组数字；舞台收紧成「主干 + 折起来的旁白」。

- **报告能交出名单与指标。** 有一类调查回答的不是「为什么」而是「哪些」——找出这个用户的小号、哪些订单被重复扣款、是哪次变更引入的。之前只能把认定的 16 个 userId 写进结论那段散文里，跟设备指纹、注册日期混在一处，问三遍「把 userId 列出来」也没用，因为压根没有第二个地方可写。现在名单单独成一节（紧接根因，一列纯文本可直接复制），指标跟在影响那节的正文旁边。两者各有一个不能省的字段：名单要说清**是不是全集**，指标要说清**是不是只是下限**——决定读的人敢不敢照着动手的正是这一句。(#14)
- **旁白挪到它所属的那张卡下面。** 之前旁白在左边独立一列、走自己的游标，一个步骤超过两行就越飘越低，最后跟它讲的那张卡差出半屏，尾卡也被推得很远。现在旁白缩进在主干卡底下，连续几行折成一个组头（`N 轮 · 第一行预览`），点组头展开，点某一行在右侧看全文并复制。默认折起来，正在跑的那组带一个活标记。(#15, #22)
- **不再有「未归类」那张卡。** 每次调查开头都会出现一张标题是「未归类」的卡：没有方向、没有结论、没有证据，而且永远关不掉——agent 根本不知道它的 id。这个词本身也不对，那不是分类失败，是「还在看，还没有假设」。它底下的调用是真的、不能丢，所以移到案件卡上（「另有 N 次调用不属于任何方向」，点开看列表）；报告的排查路径直接不列它。(#19)
- **导览图默认收起，HUD 不再挡着点。** 导览图是 HUD 唯一撑高度的东西，而 HUD 整个盒子连看着是空白的地方也吃掉点击：那一带下面的卡点不到，画布也拖不动。现在缩放条上有个「导览图」开关，收起时根本不渲染，HUD 从约 413×124 缩到 272×66，那块死区跟着消失。(#18)
- **待办卡上的语句改成只读 + 一键复制。** 这张卡整件事就是「把这条语句拿出去跑，再把结果贴回来」，而它既没有复制按钮，语句还被静默截在四行。现在是一个只读块，超高自己滚，不再截断；也不能改了——表名写错只能靠贴回来的报错自己说话，`ask_operator` 的说明与提示词都明写了这件事。另外这张卡上的单字母快捷键全部去掉且没有新的顶上：`⌘↵` 仍是当前持有键盘那张卡的主操作。(#13)
- **未决的报告留「下一步怎么查」，已解决的不再给修复建议。** 排查的 agent 手里没有修复所需的上下文，它写的方案明显弱于真正动手修的人，而一份自信却偏的方案只会把人锚住；该交接的事实（量出来的数、边界、约束）本来就在证据和结论里。「接下来该观察什么」反过来只有排查者写得出，所以这半留下。顺带修掉一个选取 bug：之前谁最后写谁生效，一个遗留步骤的后续笔记会盖住根因步骤的内容，反过来也一样，而且哪儿都不报错。(#12)
- **导出的文件名改成 `case_<调查号>_<事发日期>`。** 日期取的是事情发生那天，不是导出那天；Markdown 与长图同名。(#11)
- **修复：报告的系统时间线里证据重复两三份。** 重新关一次步骤会把整批证据再插一遍——开发库里有个步骤被关过三次，15 行时间线涨成 32 行，证据水位从 37 变成 63。现在重关是整批替换（带空数组表示「只补下一步」，不动原批次），并且整批要么全进要么全不进：一个写错的 `[call #N]` 会连同这次关闭一起退回并说明原因，不再落下半批。存量库在这次升级重投时一并修正。(#16)
- **修复：步骤详情里的时间戳换行压到正文上。** 时间列写死 82px，而 agent 写下的可能是带时区偏移的完整 ISO 串（约 173px），于是折成三行、往正文上盖 36px。现在时间列按内容定宽（带上限），带日期的时间戳显示成案件基准时区下的 `MM-DD HH:mm:ss`，原始字符串留在悬浮提示里；读不回原值的（只有时刻没有日期、非 ISO 形状、2 月 30 日这种）照 agent 写的原样显示。报告与 Markdown 导出故意不变——那是交给别人看的文档，对方不一定在同一个时区。(#23)
- **修复：工具调用的输出与它的状态现在一起落库。** 之前分两次提交，第二次失败的话输出已经在库里、调用还停在 pending，下一次清扫把它标成 abandoned 并配上占位内容，真正那份输出成了没人指向的孤儿。(#17)

**升级须知**：schema 从 v7 迁到 v9，走重放迁移，调查留在原地。v9 这一级会按新规则重投一遍证据，顺带清掉老库里重复的那几批（事件一个字节都没动）。**降级回 0.2.0 读不了这份库**：版本比代码新只能挪库，旧文件改名成 `<库名>.v9.<时间戳>.bak` 留在原地，应用另建一个空库——降级后看到的是一个没有任何调查的界面，记录还在盘上，但要回到 0.3.0 才读得到。

## [0.2.0] - 2026-08-20

新建的调查会自己跑起来；排查过程中看得见此刻在做什么。

- **新建调查后自动跑第一轮。** 之前建完停在起点卡上不动——唯一的启动入口是输入框的发送按钮，而它在输入为空时是禁用的，界面上也没有任何东西说明在等什么。现在建完即跑；第一轮起不来会明说（横幅 + 会话标记为崩溃），草稿留在输入框里，不再悄悄排进没人看的队列。(#1)
- **描述里带链接不再把标题打回兜底。** 取标题那一趟现在一个工具都不给：它只读你写下的那段文字，链接背后是什么、文件里写了什么都不去够——那是调查本身的事。之前留着一个工具入口，模型见到链接就想去抓，把这一趟的轮次用光，标题静默停在兜底那句，界面上没有任何东西说这一趟挂过。描述短到只剩一个链接时，标题会取里面认得出的标识（issue 编号、traceId、服务名、用户 ID）。(#9)
- **看得见此刻在做什么。** 运行中的步骤卡上有一条心跳（工具名、已耗时、调用与证据计数），运行中的子 agent 在支线头上挂一个提示，HUD 上有最后更新时间。秒数由界面自己走，不等快照——一次 90 秒的工具调用本来什么都不推。(#2)
- **人工回填可以拒绝。** 回填卡上多一个「不执行」：自己也没这个权限、或这条不该在生产上跑，都可以退回去，理由选填。agent 收到的是一次明确的拒绝，不会当成「查了但没数据」接着往下推，也不会把同一条原样再问一遍；轨道上那次调用记为被拒，不是跑完。(#4)
- **会话断在半路时，未决的东西都会收口。** 之前流断开或崩溃只收支线，待回填、待放行与在途调用都留在原地：一次调用会一直显示运行中直到下次启动，报告里的调用数也跟着偏高。(#3)
- **支线旁白只说轨道上看不见的事。** 跑完并留下结论的支线不再在对话带里被重复念一遍——那句话已经写在卡面上；一次调用都没打、轨道上根本看不到的那条才需要旁白托着。(#5)
- **报告排版收敛。** 根因按正文排版，不再被放大加粗成一堵字；纸面上的印记缩成一个小标签。版式不替你划重点。(#7, #8)
- **修复：舞台上的抓手光标盖住了可选中的文字。**(#6)

**升级须知**：本轮没有数据库迁移，schema 仍是 v7，与 0.1.1 是同一份库。降级回 0.1.1 也照常打开：被拒绝的回填在库里就是一次 `denied` 调用，旧版本认得它，只是界面上没有「不执行」那个手势。

## [0.1.1] - 2026-08-19

修 0.1.0 一打开就连不上 Claude 的问题。

- **修复：正式包里调查起不来。** agent 用的 CLI 被封在 asar 归档里，spawn 不出来（`spawn ENOTDIR`）。表现是新建调查面板的 Backend 与模型列表空着、点「开始排查」没有反应。只影响正式包，从源码跑的不受影响。
- **不再要求本机装 Claude Code。** 应用自带 CLI，需要的只是一个登录过的 Claude 账号（首次登录仍在终端里做一次 `claude auth login`）。设置页「关于」直接显示登录状态与账号；环境提示也换成了这件事本身，不再报「未找到 claude 可执行文件」。

## [0.1.0] - 2026-08-16

首个公开版本。

- **完整证据留存**：agent 排查过程中每一次工具调用的原始输入/输出由 harness 完整落库，agent 的文字只做索引和判断；复核一项调查靠证据本身，不靠重跑。
- **时间线即控制面**：排查实时可见，节点上直接放行 / 改写 / 拒绝 / 接管 / 停止；`ask_operator` 把要凭据或后果须由人担的操作交回给你执行，并要求 agent 先写预期再看结果。
- **两条时间线**：排查时间线按 agent 行动顺序，系统时间线按事件真实发生时间重排，同一批证据的两次投影。
- **结论可被推翻**：被推翻的结论连同证据强制留在报告里，不造一路顺利的假历史。
- **报告是投影**：章节从库里投影而来，与过程天然一致；Markdown 与长图两种导出。
- **按调查检索**：一次调查跨多个会话；数据全部落本机 SQLite，大 payload 内容寻址落盘。
- **走 Claude 订阅**：spawn 本机 `claude` CLI 继承登录凭据，已有 skill 与 MCP 配置照常加载。
- **应用内自动更新**：后台下载、退出时安装；设置页可看状态、可手动重启生效。
